[core]
src = "."
dest = "~"

[jinja]
block-start-string = "{%"
block-end-string = "%}"
variable-start-string = "{{"
variable-end-string = "}}"

[vars]
editor = "vim"

additional_paths = [
   "$HOME/local/bin",
   "$HOME/.cargo/bin",
]
